[build]
  base = "project/frontend-challenge"
  publish = "build"
  command = "npm ci && npm run build"

[build.environment]
  NODE_VERSION = "18"

# Ensure SPA routes work (React Router)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200


